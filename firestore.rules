rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application access goes through authenticated Next.js Admin SDK routes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
